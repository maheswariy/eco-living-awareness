<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= blog.category %> - Full Blog</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Source+Sans+Pro:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary-green: #1f6f3d;
      --secondary-green: #2e8b57;
      --light-green: #f2fef4;
      --dark-green: #14532d;
      --accent: #ff9a3c;
    }
    
    body {
      font-family: 'Source Sans Pro', sans-serif;
      background-color: var(--light-green);
      margin: 0;
      padding: 40px;
      color: #333;
      line-height: 1.6;
    }

    .blog-container {
      max-width: 800px;
      margin: auto;
      background-color: white;
      padding: 40px;
      box-shadow: 0 5px 25px rgba(0,0,0,0.08);
      border-radius: 12px;
      position: relative;
      overflow: hidden;
    }

    .blog-container::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 8px;
      background: linear-gradient(90deg, var(--primary-green), var(--secondary-green), var(--accent));
    }

    h1 {
      color: var(--primary-green);
      font-family: 'Playfair Display', serif;
      font-size: 2.5rem;
      margin-bottom: 15px;
      line-height: 1.3;
      position: relative;
      padding-bottom: 15px;
    }

    h1::after {
      content: "";
      position: absolute;
      bottom: 0;
      left: 0;
      width: 80px;
      height: 3px;
      background-color: var(--accent);
    }

    .meta {
      color: #666;
      font-size: 0.9rem;
      margin-bottom: 25px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .meta::before {
      content: "•";
      color: var(--secondary-green);
      font-size: 1.2rem;
    }

    .blog-image {
      width: 100%;
      height: 400px;
      object-fit: cover;
      border-radius: 10px;
      margin-bottom: 30px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.1);
      transition: transform 0.3s ease;
    }

    .blog-image:hover {
      transform: scale(1.01);
    }

    p {
      font-size: 1.1rem;
      line-height: 1.8;
      color: #444;
      margin-bottom: 25px;
    }

    p:first-of-type::first-letter {
      font-family: 'Playfair Display', serif;
      font-size: 3.5rem;
      float: left;
      line-height: 0.8;
      margin-right: 10px;
      color: var(--primary-green);
      margin-top: 5px;
    }

    .back-btn {
      margin-top: 40px;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 25px;
      background-color: var(--primary-green);
      color: white;
      text-decoration: none;
      border-radius: 8px;
      transition: all 0.3s ease;
      font-weight: 600;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .back-btn:hover {
      background-color: var(--dark-green);
      transform: translateY(-2px);
      box-shadow: 0 6px 12px rgba(0,0,0,0.15);
    }

    .back-btn::before {
      content: "←";
      font-size: 1.1rem;
    }

    .tags {
      display: flex;
      gap: 10px;
      margin: 30px 0;
      flex-wrap: wrap;
    }

    .tag {
      background-color: #e0f5e9;
      color: var(--dark-green);
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
    }

    @media (max-width: 768px) {
      body {
        padding: 20px;
      }
      
      .blog-container {
        padding: 25px;
      }
      
      h1 {
        font-size: 2rem;
      }
      
      .blog-image {
        height: 250px;
      }
    }
  </style>
</head>
<body>
  <div class="blog-container">
    <h1><%= blog.category %></h1>
    <div class="meta">Posted by: <%= blog.email %></div>
    
    <% if (blog.image) { %>
      <img src="<%= blog.image %>" alt="<%= blog.category %>" class="blog-image">
    <% } %>
    
    <% if (blog.tags && blog.tags.length) { %>
      <div class="tags">
        <% blog.tags.forEach(tag => { %>
          <span class="tag"><%= tag %></span>
        <% }); %>
      </div>
    <% } %>
    
    <p><%= blog.content %></p>
    
    <a href="/blogs" class="back-btn">Back to Blogs</a>
  </div>
</body>
</html>